
import React from 'react';
import BlogPodcastManager from '../BlogPodcastManager';

const BlogPodcastView: React.FC = () => {
  return (
    <div className="space-y-6">
      <BlogPodcastManager />
    </div>
  );
};

export default BlogPodcastView;
